el numero delante de la carpeta indica cuál es la ultima ejecución, el numero mas alto corresponde a la ultima ejecución hecha.

En el resultado 3 se prueba con 40 épocas y los parámetros: 

results = model.train(
    data=DATA_YAML,
    epochs=40,
    patience=10,             # early stopping
    imgsz=640,
    batch=32,                # si peta, usa 16
    lr0=0.0005,              # mejor para AdamW
    optimizer="AdamW",       
    device=device,
    name="ppe_yolov8_finetuned_mod_v2",
    pretrained=True,
    workers=4,
    augment=True,            # activar aumentos
    #freeze=10                # esto no lo he usado pero puede ser interesante explorar
)